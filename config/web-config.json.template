{
  "config": {
    "url": "${KEYCLOAK_URL}",
    "realm": "${KEYCLOAK_REALM}",
    "clientId": "${KEYCLOAK_WEB_CLIENT_ID}"
  },
  "initOptions": {
    "onLoad": "check-sso",
    "silentCheckSsoRedirectUri": "${KEYCLOAK_WEB_REDIRECT_URI}"
  }
}